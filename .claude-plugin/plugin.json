{
  "name": "local-memory",
  "version": "0.1.0",
  "description": "Persistent, searchable memory for Claude Code that runs entirely on your subscription: hooks log what happens, your own session writes typed observations at checkpoints, and a local SQLite index (BM25 + optional vectors) injects only the relevant past into each prompt. No background workers, no API keys.",
  "author": {
    "name": "Prakash Acharya"
  },
  "keywords": ["memory", "rag", "sqlite", "hooks", "context"]
}
